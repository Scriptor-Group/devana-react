import React from "react";
interface IProps {
    metadata?: any;
    children?: React.ReactNode;
    aiId: string;
    chatId?: string;
}
export declare const Conversation: React.FC<IProps>;
export {};
